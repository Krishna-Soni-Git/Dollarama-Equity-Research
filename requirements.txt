# =============================================================================
# Dollarama Equity Research Dashboard — Python Dependencies
# Updated: March 2026
# Tested on: Python 3.10, 3.11, 3.12 | Windows, macOS, Linux
#
# Install:
#   pip install -r requirements.txt
#
# Requires Python 3.9 or higher.
# =============================================================================

# ── Dashboard framework ───────────────────────────────────────────────────────
streamlit>=1.40.0,<2.0.0

# ── Charts ────────────────────────────────────────────────────────────────────
plotly>=5.20.0,<7.0.0

# ── Data manipulation ─────────────────────────────────────────────────────────
pandas>=2.2.0

# ── Numerical computing ───────────────────────────────────────────────────────
numpy>=1.26.0

# ── Plotting — required for pandas .background_gradient(cmap=...) ─────────────
# Used in Tab 1 (income statement table) and Tab 11 (raw data table)
# to colour-code EBITDA and margin columns with a green gradient.
matplotlib>=3.7.0,<4.0.0

# ── Live financial data ───────────────────────────────────────────────────────
# Pinned below 1.0.0 — yfinance 1.x requires curl_cffi (needs C compiler)
# which fails on Windows/older macOS without build tools installed.
yfinance>=0.2.50,<1.0.0

# ── Machine learning (ML tab — Tutorial 5) ────────────────────────────────────
scikit-learn>=1.3.0

# ── Optional: loads .env file for local config ────────────────────────────────
python-dotenv>=1.0.0