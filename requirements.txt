# =============================================================================
# Dollarama Equity Research Dashboard — Python Dependencies
# Updated: March 2026
# Tested on: Python 3.10, 3.11, 3.12 | Windows, macOS, Linux
#
# Install all at once:
#   pip install -r requirements.txt
#
# Requires Python 3.9 or higher.
# =============================================================================

# ── Dashboard framework ───────────────────────────────────────────────────────
streamlit>=1.40.0,<2.0.0

# ── Interactive charts ────────────────────────────────────────────────────────
plotly>=5.20.0,<7.0.0

# ── Data manipulation ─────────────────────────────────────────────────────────
pandas>=2.2.0

# ── Numerical computing + Monte Carlo simulation ──────────────────────────────
numpy>=1.26.0

# ── Colour gradients in tables (pandas .background_gradient) ─────────────────
# Required by Tab 1 (income statement) and Tab 11 (raw data) for green
# gradient colouring on margin columns. Pandas uses matplotlib.cm internally.
matplotlib>=3.7.0,<4.0.0

# ── Live financial data (Dollarama only — peers use verified 10-K data) ───────
# Pinned below 1.0.0: yfinance 1.x introduced curl_cffi which requires a
# C compiler and fails silently on Windows / older macOS without build tools.
yfinance>=0.2.50,<1.0.0

# ── Machine learning — Tab 9 (Tutorial 5) ────────────────────────────────────
scikit-learn>=1.3.0

# ── Optional: loads .env file for local environment variables ─────────────────
python-dotenv>=1.0.0